# `drill -q -s -b drill.yml`
concurrency: 10
base: "http://localhost:8080"
iterations: 2000
rampup: 2

plan:
  - name: List Games
    request:
      url: /api/games

  - name: Get a Game
    request:
      url: /api/games/Ghostrunner

  - name: Healthcheck
    request:
      url: /api/health
