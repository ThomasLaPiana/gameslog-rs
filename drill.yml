# `drill -q -s -b drill.yml`
concurrency: 5
base: 'http://localhost:8080'
iterations: 500
rampup: 2

plan:
  - name: List Games
    request:
      url: /api/games

  - name: Get a Game
    request:
      url: /api/games/Ghostrunner
