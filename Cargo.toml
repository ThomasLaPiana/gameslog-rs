[package]
name = "gameslog"
version = "0.1.0"
authors = ["ThomasLaPiana"]
edition = "2021"
publish = false

default-run = "gameslog"

[workspace]
members = [".", "cli", "webserver", "entity", "migration"]
default-members = [".", "webserver", "cli"]

[dependencies]
gameslog-api = { path = "webserver" }
